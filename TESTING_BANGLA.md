# Voice Input — টেস্ট করার নিয়ম (বাংলা)

## ০. লাগবে যা যা
- macOS
- Chrome / Brave / Edge — যেটাতে আগে থেকে chatgpt.com এ লগইন করা আছে
- Node.js ইনস্টল করা

---

## ১. অ্যাপ চালু করা

টার্মিনাল খুলে প্রজেক্ট ফোল্ডারে যান, তারপর dependency ইনস্টল করে অ্যাপ চালান:

```bash
npm install
npm run dev
```

কিছুক্ষণ পর অ্যাপের মেইন উইন্ডো খুলবে আর ভেতরে ChatGPT লোড হবে।

---

## ২. লগইন (Cookie ইমপোর্ট)

- যদি ChatGPT লগইন না থাকে, নিচের বার থেকে **Connect Browser Session** (Import Cookie) বাটনে ক্লিক করুন।
- আপনার ব্রাউজার (Chrome/Brave/Edge) সিলেক্ট করুন।
- এবার ChatGPT লগইন অবস্থায় চলে আসবে।

---

## ৩. Readiness (অ্যাপ রেডি হওয়া)

- অ্যাপ চালু হলে নিচের মাইক বাটন প্রথমে **disabled** থাকবে আর স্ট্যাটাসে **Preparing** দেখাবে।
- ChatGPT পুরো লোড হলে স্ট্যাটাস **Ready** হবে আর মাইক বাটন কাজ করা শুরু করবে।
- যদি ৬০ সেকেন্ডের মধ্যে রেডি না হয়, অ্যাপ নিজে থেকে webview রিলোড করবে (সর্বোচ্চ ৫ বার চেষ্টা করবে)।

> মাইক/Accessibility পারমিশন চাইলে **Allow** দিন।

---

## ৪. রেকর্ডিং শুরু আর বন্ধ (মূল ফিচার)

- যেকোনো জায়গা থেকে **Cmd + Shift + R** চাপুন → রেকর্ডিং শুরু হবে।
- স্ক্রিনের বাম-নিচে একটা ছোট **floating pill** দেখাবে — সেখানে **Recording** লেখা আসবে।
- মেনুবারে (উপরে) **⏺** চিহ্ন দেখাবে।
- এবার কিছু কথা বলুন (ইংরেজি/বাংলা যেটাতে ChatGPT dictation কাজ করে)।
- আবার **Cmd + Shift + R** চাপুন → রেকর্ডিং বন্ধ হবে আর ট্রান্সক্রিপশন শুরু হবে।

---

## ৫. টেক্সট কোথায় যাবে — Output Mode

নিচের বারে **Clipboard / Paste** টগল আছে। দুইটাই টেস্ট করুন:

- **Clipboard mode:** রেকর্ড বন্ধ করার পর লেখা ক্লিপবোর্ডে চলে যাবে। যেকোনো জায়গায় **Cmd + V** দিয়ে paste করে দেখুন লেখা এসেছে কিনা।
- **Paste mode:** রেকর্ড করার আগে অন্য কোনো অ্যাপে (যেমন Notes/TextEdit) কার্সার রাখুন। রেকর্ড বন্ধ করলে লেখা নিজে থেকে ওই কার্সারের জায়গায় paste হয়ে যাবে।
  - প্রথমবার **Accessibility** পারমিশন চাইতে পারে → System Settings → Privacy & Security → Accessibility তে গিয়ে অ্যাপটা allow করুন। পারমিশন না দিলে লেখা ক্লিপবোর্ডে থেকে যাবে (হারাবে না)।

ট্রান্সক্রিপশন কপি/paste হওয়ার পর webview নিজে থেকে রিলোড হবে — এটা স্বাভাবিক।

---

## ৬. "Wait more?" প্রম্পট টেস্ট

- রেকর্ড বন্ধ করার পর যদি ৫ মিনিটের মধ্যে কোনো লেখা না আসে, floating pill এ একটা প্রম্পট আসবে: **Wait / No / Record again**।
- **Wait** → আরও অপেক্ষা করবে।
- **No** → বাতিল করে idle তে ফিরবে।
- **Record again** → রিলোড করে আবার রেকর্ড শুরু করবে।

---

## ৭. ChatGPT তে মেসেজ পাঠানো (অতিরিক্ত)

- **Cmd + Shift + D** চাপলে কম্পোজারের লেখা ChatGPT তে সেন্ড হবে আর রিপ্লাই পোল করবে।

---

## ৮. Tray / Menubar টেস্ট

- মেনুবারের মাইক আইকনে ক্লিক করুন। মেনুতে থাকবে: **Show/Hide**, **Connect Browser Session**, **Quit**।
- উইন্ডো minimize করলে সেটা ট্রে তে হাইড হবে, পুরো বন্ধ হবে না।

---

## ৯. Quit

- ট্রে মেনু থেকে **Quit** দিলে অ্যাপ পুরোপুরি বন্ধ হবে।

---

## দ্রুত চেকলিস্ট

1. `npm install` → `npm run dev` দিয়ে অ্যাপ ওপেন হলো ✅
2. Cookie ইমপোর্ট করে ChatGPT লগইন হলো ✅
3. স্ট্যাটাস Preparing → Ready হলো, মাইক enable হলো ✅
4. Cmd+Shift+R → Recording pill + মেনুবারে ⏺ ✅
5. আবার Cmd+Shift+R → লেখা ক্লিপবোর্ডে/paste হলো ✅
6. Clipboard আর Paste দুই mode-ই কাজ করলো ✅
7. webview নিজে থেকে রিলোড হলো ✅
8. Tray এর Show/Hide আর Quit কাজ করলো ✅
